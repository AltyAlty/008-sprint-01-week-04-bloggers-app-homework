Урок: https://github.com/it-incubator/ed-back-sprint-1-lesson-4

/*--------------------------------------------------------------------------------------------------------------------*/

1. Инициализация проекта: pnpm init
2. Установка всех зависимостей: pnpm install
3. Установка Express: pnpm add express
4. Установка TypeScript, Jest, TS-Jest, Supertest, ESLint, Prettier, eslint-config-prettier, eslint-plugin-prettier, nodemon:
pnpm add -D typescript @types/express jest ts-jest @types/jest @types/node supertest @types/supertest eslint @typescript-eslint/eslint-plugin @typescript-eslint/parser prettier eslint-config-prettier eslint-plugin-prettier nodemon
5. Инициализация файла "tsconfig.json": pnpm tsc --init
6. Установка swagger-jsdoc и swagger-ui-express: pnpm add swagger-jsdoc swagger-ui-express
7. Установка типов для swagger-jsdoc и swagger-ui-express: pnpm add -D @types/swagger-jsdoc @types/swagger-ui-express
8. Установка express-validator: pnpm add express-validator
9. Установка драйвера MongoDB для Node.js: pnpm add mongodb
10. Установка библиотеки Dotenv: pnpm add -D dotenv

/*--------------------------------------------------------------------------------------------------------------------*/

Скрипты:
1. pnpm watch: компиляция файлов TypeScript из папки "src" в папку "dist" в режиме наблюдения (-w).
2. pnpm dev: запуск приложения с помощью nodemon, следя за изменениями в скомпилированном коде в папке "dist".
3. pnpm jest: запуск тестов с помощью Jest.
4. pnpm format: форматирование кода с помощью Prettier.
5. pnpm lint: запуск проверки кода с помощью ESLint и автоматического их исправления.
6. pnpm build: компиляция файлов TypeScript из папки "src" в папку "dist", нужно для деплоя на vercel.com.
7. pnpm start: запуск приложения, нужно для деплоя на vercel.com.

/*--------------------------------------------------------------------------------------------------------------------*/

Файл "package.json": настройка полей "main" и "scripts".
Файл "tsconfig.json": ручная настройка.
Файл "jest.config.json" создан вручную. Запускаются тесты только из папки "__tests__/e2e/".

Файл "tsconfig.test.json" создан вручную. Содержит отдельные правила для тестов.
1. Берет все базовые настройки из файла "tsconfig.json".
2. TypeScript проверяет типы, но не генерирует файлы .js.
3. Корнем проекта для этого конфига становится папка проекта (где лежит tsconfig). Это позволяет включать и папку "src",
и папку "__tests__", даже если они на одном уровне, и убирает конфликт вида “файл вне rootDir”.
4. В тестовый проект входят весь код приложения из папки "src" и все тесты из папки "__tests__".

Файл "eslint.config.mjs" создан вручную. Правила в этом файле:
1. Игнорирует папки "node_modules" и "dist".
2. Использует TS парсер.
3. Использует TS плагин и Prettier плагин.
4. Проверяет только файлы .ts
5. Показывает ошибку если форматирование не по правилам Prettier.
6. Показывает предупреждение для неиспользуемых переменных.
7. Показывает ошибку если используется == или !=.
8. Отключает правила ESLint, которые конфликтуют с Prettier.

Файл ".prettierrc" создан вручную. Правила в этом файле:
1. В конце ставятся точки с запятой.
2. Используются одинарные кавычки вместо двойных.
3. Используются запятые при перечислении, в том числе в конце.
4. Перенос строк длиннее 120 символов.
5. Отступ - 2 пробела.

Файл ".prettierignore" создан вручную. Правила в этом файле: Prettier игнорирует папки "node_modules" и "dist", а также
файл "pnpm-lock.json".

/*--------------------------------------------------------------------------------------------------------------------*/

Настройка Prettier в WebStorm: Settings -> Languages & Frameworks -> JavaScript -> Prettier -> Manual Prettier
Configuration -> Run on 'Reformat Code' action - ON, Apply Prettier to files outside the prettier dependency scope -
OFF, Run for files - add "json", Run on save - ON.

Настройка ESLint в WebStorm: Settings -> Languages & Frameworks -> JavaScript -> Code Quality Tools -> ESLint ->
Automatic ESLint configuration - ON, Run eslint --fix on save - ON.

/*--------------------------------------------------------------------------------------------------------------------*/

Настройка Swagger:
1. Файл "/src/core/swagger/setup-swagger.ts" - создание функции для инициализации документации Swagger.
2. Файл "/src/setup-app.ts" - вызов функции для инициализации документации Swagger.
3. Во всех роутерах добавить путь "/app/".
4. Во всех тестах добавить путь "/app/".
5. Создать вручную файлы .yml, описывающие документацию Swagger:
5.1 Файл "/src/core/swagger/openapi.swagger.yml".
5.2 Файл "/src/blogs/docs/blogs.swagger.yml".
5.3 Файл "/src/posts/docs/posts.swagger.yml".
5.4 Файл "/src/testing/docs/testing.swagger.yml".

Настройка деплоя приложения на vercel.com:
1. Создать файл "vercel.json".
2. В файл "package.json" добавить скрипты:
2.1 "build": "tsc".
2.2 "start": "node dist/index.js".
3. Залогиниться на vercel.com.
4. Выбрать "Add New..." - "Project".
5. Выбрать "Continue with GitHub".
6. Импортировать нужный репозиторий.
7. Указать "Application Preset" как "Express".
8. Указать "Build Command" как "pnpm build".
9. Указать "Output Directory" как "dist".
10. Указать "Install Command" как "pnpm install".

Настройка Swagger, чтобы он корректно отображался и работал на vercel.com:
1. Использовать файл "setup-swagger.ts" вместо файла "setup-swagger-original.ts".
2. В файле "vercel.json" добавить, чтобы не игнорировались файлы .yml:
"builds": [
    {
      "config": {
        "includeFiles": ["src/**/*.swagger.yml"]
      }
    }
  ]

Документация Swagger находиться по пути "/api".

/*--------------------------------------------------------------------------------------------------------------------*/

Jest - тестовый фреймворк для JavaScript, который поддерживает тестирование кода. Используется для написания и
выполнения юнит-тестов, тестов интеграции и моков.

TS-Jest - преобразователь для Jest, позволяющий тестировать TypeScript-код. Позволяет Jest работать с TypeScript,
компилируя его во время тестирования.

Supertest - библиотека для e2e тестирования HTTP-запросов. Используется для тестирования API, отправляя запросы к
серверу и проверяя ответы.

ESLint - линтер для JavaScript и TypeScript. Используется для анализа кода на наличие ошибок и проблем с качеством,
таких как несоответствие стилю кодирования или потенциальные ошибки.

Prettier - форматировщик кода. Используется для автоматического форматирования кода по установленным правилам, улучшая
читаемость и единообразие.

eslint-config-prettier - конфигурация для ESLint, отключающая правила, которые могут конфликтовать с Prettier. Помогает
интегрировать Prettier с ESLint без конфликтов, отключая проверки, которые дублируют функциональность Prettier.

eslint-plugin-prettier - плагин для ESLint, который интегрирует Prettier в процесс линтинга. Обеспечивает использование
Prettier как ESLint-правила, что позволяет линтить код и форматировать его в одном процессе.

swagger-jsdoc - инструмент для генерации документации API в формате Swagger на основе JSDoc комментариев в коде. Он
помогает автоматически генерировать документацию API в стандартном формате (Swagger), что упрощает поддержку и
обновление документации по мере изменения кода.

swagger-ui-express - среда для Express, которая позволяет отображать документацию Swagger в красивом веб-интерфейсе. Она
предоставляет веб-интерфейс, где разработчики могут просматривать и взаимодействовать с документацией API, что позволяет
тестировать API прямо из браузера.

Dotenv - это модуль, не зависящий ни от одного компонента, который загружает переменные окружения из файла ".env" в файл
"process.env".

/*--------------------------------------------------------------------------------------------------------------------*/

"process" - это глобальный объект в Node.js, который содержит информацию о текущем процессе выполнения. "process.env" -
это объект, содержащий все переменные окружения, доступные приложению. Переменные окружения - это значения, которые
могут быть установлены на уровне операционной системы или приложения. Переменные окружения могут использоваться для
настройки поведения программного обеспечения (например, пароли, ключи API, пути к файлам и так далее).

/*--------------------------------------------------------------------------------------------------------------------*/

Метод "sendStatus()" используется для отправки только статуса без тела ответа.

/*--------------------------------------------------------------------------------------------------------------------*/

Data Transfer Object - это объект, определяющий форму и структуру данных, которыми обмениваются разные части приложения
или разные сервисы.

/*--------------------------------------------------------------------------------------------------------------------*/

Слой Repository - это один из паттернов проектирования, позволяющий разделить логику взаимодействия с базой данных от
бизнес-логики. Это дает следующие преимущества:
1. Чистота и структура кода: роутеры избавляются от логики работы с базой.
2. Легкость тестирования: репозиторий можно тестировать независимо от остальных частей приложения.
3. Гибкость: если структура или тип БД изменится, все изменения нужно будет сделать только в репозитории, не затрагивая
другие части приложения.

/*--------------------------------------------------------------------------------------------------------------------*/

Middlewares в Express.js могут получать три параметра:
1. Объект запроса.
2. Объект ответа.
3. Функция "next()" для передачи управления следующему обработчику.

/*--------------------------------------------------------------------------------------------------------------------*/

Basic authorization - это способ аутентификации в веб-приложениях, при котором клиент передает серверу логин и пароль в
виде строки, закодированной в формате base64. Этот метод не является безопасным, если не используется с протоколом
HTTPS. Порядок работы этого способа примерно такой:
1. Клиент при отправке запроса должен указать в заголовке HTTP следующее:
Authorization: Basic <base64-encoded-credentials>
2. Чтобы получить строку для Basic авторизации, нужно соединить логин и пароль через двоеточие, а затем закодировать
результат в base64.
3. Сервер извлекает данные, декодирует их и проверяет логин и пароль. Если они верны, то сервер разрешает доступ.

/*--------------------------------------------------------------------------------------------------------------------*/

Создание переменных окружения при помощи библиотеки Dotenv:
1. Создать файл ".env" в корне проекта.
2. Указать этот файл в файле ".gitignore".
3. В файле точки входа в приложение (index.ts) и в каждом файле с тестами обязательно самой первой строкой добавить:
"import 'dotenv/config';".
4. В файле ".env" указать нужные переменные, используя двойные кавычки и не используя пробелы.

/*--------------------------------------------------------------------------------------------------------------------*/

Реляционные БД хранят данные в виде таблиц с четкими связями между ними. Документоориентированные БД хранят данные в
виде документов.

Создание MongoDB БД при помощи подключения к MongoDB Atlas на сайте mongodb.com:
1. Зарегистрироваться на сайте.
2. Создать кластер.
2.1 Выбрать "Free".
2.2 Задать имя кластеру.
2.3 Провайдер и регион будут выбраны автоматически.
2.4 Нажать кнопку "Create Deployment".
3. Создать пользователя БД.
3.1 Задать имя и пароль для пользователя БД.
3.2 Нажать "Create Database User".
3.4 Нажать "Choose a connection method".
3.5 Выбрать "Drivers".
3.6 В поле "Driver" выбрать "Node.js".
3.7 В поле "Version" выбрать "6.7 or later".
3.8 Скопировать connection string: "mongodb+srv://username:password@cluster0.yi738es.mongodb.net/?appName=Cluster0".
3.9 Нажать "Done".
4. Разрешить доступ к БД с любого IP-адреса.
4.1 Выбрать раздел "Security/Database & Network Access".
4.2 Выбрать раздел "Network Access/IP Access List".
4.3 Нажать "+ADD IP ADDRESS".
4.4 В поле "Access List Entry" ввести "0.0.0.0/0".
4.5 Нажать "Confirm".
4.6 Другие IP-адреса в списке можно удалить.
5. В разделе "Database" можно работать с БД.
6. Использовать connection string как Mongo URL.

Создание БД MongoDB локально (архив MongoDB + архив СУБД MongoDB Shell):
1. Скачать MongoDB в формате .zip с сайта mongodb.com/try/download/community.
2. Разархивировать архив в отдельную папку.
3. Запустить локальный сервер БД при помощи файла "mongod.exe".
3.1 Открыть окно терминала или окно PowerShell в папке "bin".
3.2 В папке "bin" выполнить команду "./mongod.exe --dbpath .\data\db", где ".\data\db" это путь, где мы хотите создать
саму БД (папки должны быть уже созданы). В нашем случае используется такие команды:
PowerShell:
./mongod.exe --dbpath ..\..\..\..\..\DevProjects\IT-IncubatorBackEndProjects\006-sprint-01-week-03-bloggers-app-homework\src\db\mongodb\data\db
Командная строка:
mongod --dbpath ..\..\..\..\..\DevProjects\IT-IncubatorBackEndProjects\006-sprint-01-week-03-bloggers-app-homework\src\db\mongodb\data\db
4. Запустить СУБД MongoDB Shell.
4.1 Скачать MongoDB Shell в формате .zip с сайта www.mongodb.com/try/download/shell.
4.2 Файл "mongosh.exe" из архива скопировать в папку "bin".
4.3 В папке "bin" выполнить команду "./mongosh.exe" в PowerShell, или команду "mongosh" в командной строке.

Создание БД MongoDB локально (MongoDB + СУБД MongoDB Compass):
1. Скачать MongoDB в формате .msi с сайта mongodb.com/try/download/community.
2. Во время установки выбрать тип "Complete".
3. Во время установки убрать галочку "Install MongoDB as a Service".
4. Во время установки выбрать галочку "Install MongoDB Compass".

Установка и запуск СУБД MongoDB Compass отдельно:
1. Скачать MongoDB Compass с сайта mongodb.com/try/download/compass.
2. Установить и запустить MongoDB Compass.
3. Выбрать пункт "Add new connection".
4. В разделе "URI" указать "mongodb://localhost:27017".
5. В разделе "Name" указать имя соединения, например, "006-sprint-01-week-03-bloggers-app-homework".
6. Выбрать "Advanced Connection Options".
7. В разделе "Host" указать "localhost:27017".
8. Нажать "Save & Connect".

Установка и запуск СУБД Studio 3T Community Edition:
1. Скачать Studio 3T Community Edition с сайта robomongo.org.
2. Установить и запустить Studio 3T Community Edition.
3. Выбрать пункт "Create a new connection".
4. Выбрать пункт "Manually configure my connection settings".
5. В разделе "Server" указать "localhost".
6. В разделе "Port" указать "27017".
7. В разделе "Connection name" указать имя соединения, например, "006-sprint-01-week-03-bloggers-app-homework".
8. Нажать "Save".
9. Выбрать соединение и нажать "Connect".

Создание БД MongoDB локально с помощью Docker:
0. Просмотреть инструкцию https://docs.google.com/document/d/1VPkumb8yHz6W4SH6suxjstE89qhLWZbHrv6OF8Je_Nc/edit?tab=t.0.
1. Скачать Docker Desktop с сайта docker.com/products/docker-desktop.
2. Установить и запустить Docker Desktop.
3. В поиске на сайте hub.docker.com вбить "mongo" и найти MongoDB образ для Docker.
4. На странице образа нажать "Run in Docker Desktop".
5. В Docker Desktop в поле "Tag" указать "latest".
6. Нажать "Pull".
7. В поле "Container name" указать имя контейнера.
8. В поле "Host" указать "27017".
9. Нажать "Run".
10. Далее этот образ можно найти во вкладке "Images" в Docker Desktop.

/*--------------------------------------------------------------------------------------------------------------------*/

Чтобы временно развернуть приложение на удаленном сервере, например, если БД развернута локально, то можно использовать
туннели:
1. serveo.net (команда на сайте).
2. localhost.run (команда на сайте).
3. pinggy.io (команда на сайте).
4. ngrok.com (требуется регистрация и установка dev зависимостей или приложения).
5. theboroer.github.io/localtunnel-www (требуется установка dev зависимости).
6. tuna.am.

Инструкция для tuna.am:
1. Зарегистрироваться на сайте tuna.am.
2. Запустить команду через PowerShell "winget install --id yuccastream.tuna ; powershell".
3. Перезапустить PowerShell.
4. Запустить back-end приложение.
5. Запустить команду через PowerShell "tuna config save-token your_token", где "your_token" это токен, который можно
взять на сайте.
6. Запустить команду через PowerShell "tuna http port", где "port" это порт, на котором запущено back-end приложение.

/*--------------------------------------------------------------------------------------------------------------------*/

OpenAPI 3.0 - это спецификация для описания RESTful API в формате JSON или YAML.

/*--------------------------------------------------------------------------------------------------------------------*/

Структура приложения:
0.1 Файл "/src/index.ts" - точка входа приложения.
0.2 Файл "/src/setup-app.ts" - настройка приложения.

1. "/src/auth/" - код, связанный с авторизацией.
1.1 "/src/auth/middlewares/" - middlewares, связанные с авторизацией.

2. "/src/core/" - общий код для всего приложения.
2.1 "/src/core/errors/" - код, связанный с ошибками для разных частей приложения.
2.2 "/src/core/middlewares/" - middlewares для всего приложения.
2.2.1 "/src/core/middlewares/validation/" - валидационные middlewares для всего приложения.
2.3 "/src/core/settings/" - глобальные настройки для всего приложения.
2.4 "/src/core/swagger/" - настройка Swagger для всего приложения.
2.5 "/src/core/types/" - общие типы для всего приложения.
2.5.1 "/src/core/types/domain/" - доменные типы для всего приложения.
2.5.2 "/src/core/types/pagination/" - типы для пагинации для всего приложения.
2.5.3 "/src/core/types/validation/" - типы для валидации для всего приложения.
2.6 "/src/core/utils/" - утилиты для всего приложения.
2.6.1 "/src/core/utils/" - утилиты для пагинации для всего приложения.

3. "/src/db/" - код, связанный с БД.
3.1 "/src/db/mongodb" - код, связанный с MongoDB.
3.2 "/src/db/mongodb/data/db/" - локальная БД MongoDB.

4. "/src/drivers/" - код, связанный с водителями.
4.1 "/src/drivers/application/" - код, связанный с BLL (сервисы для работы с данными по водителям).
4.1.1 "/src/drivers/application/dto/" - DTO, связанные с BLL.
4.2 "/src/drivers/docs/" - документация Swagger по водителям.
4.3 "/src/drivers/repositories/" - код, связанный с DAL (репозитории для работы с данными по водителям в БД).
4.3.1 "/src/drivers/repositories/mappers/" - функции-преобразователи для работы с DAL.
4.4 "/src/drivers/routers/" - код, связанный с UI (роутер и маршруты для работы с данными по водителям).
4.4.1 "/src/drivers/routers/handlers/" - функции-обработчики для роутера для работы с данными по водителям.
4.4.2 "/src/drivers/routers/input-dto/" - DTO для входящих данных по водителям.
4.4.3 "/src/drivers/routers/output-dto/" - DTO для исходящих данных по водителям.
4.5 "/src/drivers/types/" - типы для водителей.
4.6 "/src/drivers/validation/" - валидационные middlewares для входных данных по водителям.

5. "/src/rides/" - код, связанный с поездками.
5.1 "/src/rides/application/" - код, связанный с BLL (сервисы для работы с данными по поездкам).
5.1.1 "/src/rides/application/dto/" - DTO, связанные с BLL.
5.2 "/src/rides/docs/" - документация Swagger по поездкам.
5.3 "/src/rides/repositories/" - код, связанный с DAL (репозитории для работы с данными по поездкам в БД).
5.3.1 "/src/rides/repositories/mappers/" - функции-преобразователи для работы с DAL.
5.4 "/src/rides/routers/" - код, связанный с UI (роутер и маршруты для работы с данными по поездкам).
5.4.1 "/src/rides/routers/handlers/" - функции-обработчики для роутера для работы с данными по поездкам.
5.4.2 "/src/rides/routers/input-dto/" - DTO для входящих данных по поездкам.
5.4.3 "/src/rides/routers/output-dto/" - DTO для исходящих данных по поездкам.
5.5 "/src/rides/types/" - типы для поездок.
5.6 "/src/rides/validation/" - валидационные middlewares для входных данных по поездкам.

6. "/src/testing/" - код, связанный с тестированием приложения.
6.1 "/src/testing/docs/" - документация Swagger по тестированию.
6.2 "/src/testing/routers/" - роутер для тестирования приложения.

7. "/__tests__/" - тестирование приложения.
7.1 "/__tests__/e2e/" - e2e-тесты.
7.1.1 "/__tests__/e2e/drivers/" - e2e-тесты по водителям.
7.1.2 "/__tests__/e2e/rides/" - e2e-тесты по поездкам.
7.2 "/__tests__/utils/" - утилиты, связанные с тестированием приложения.
7.2.1 "/__tests__/utils/auth/" - утилиты по работе с авторизацией, связанные с тестированием приложения.
7.2.2 "/__tests__/utils/db/" - утилиты по работе с БД, связанные с тестированием приложения.
7.2.3 "/__tests__/utils/drivers/" - утилиты, связанные с тестированием водителей.
7.2.4 "/__tests__/utils/rides/" - утилиты, связанные с тестированием поездок.

/*--------------------------------------------------------------------------------------------------------------------*/